{
  "name": "voice-scale-e2e",
  "private": true,
  "description": "実ブラウザでの動作確認だけに使う。docs/ の配信物は外部依存なしで動く。",
  "scripts": {
    "test": "node tests/e2e/run.mjs"
  },
  "devDependencies": {
    "playwright": "^1.62.0"
  }
}
